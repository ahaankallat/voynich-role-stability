[project]
name = "voynich-role-stability"
version = "0.4.0"
description = "Reproducible role stability analysis for Voynichese"
requires-python = ">=3.10"
dependencies = ["numpy", "pandas", "matplotlib", "scikit-learn"]
