# Overleaf instructions

Upload the source only ZIP to Overleaf and set `main.tex` as the root document.

The ZIP contains LaTeX source, tables, figures, and references. It does not need compiled auxiliary files.
