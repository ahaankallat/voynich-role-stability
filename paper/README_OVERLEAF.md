# Overleaf and arXiv source package

Upload this ZIP directly to Overleaf or arXiv source upload and set `main.tex` as the root document.

This package is source only. It intentionally excludes compiled PDF, auxiliary files, logs, and build outputs.
