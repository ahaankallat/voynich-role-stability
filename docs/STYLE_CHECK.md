# Style check

The prose was written to avoid em dashes, semicolons, and literal colons in `paper/main.tex`.

Table labels and PDF viewer generated labels may still display punctuation automatically.
